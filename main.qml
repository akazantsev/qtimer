import QtQuick 2.5
import QtQuick.Controls 1.3

ApplicationWindow {
    id: root

    title: "QTimer"
    visible: true
    minimumWidth: content.implicitWidth
    minimumHeight: content.implicitHeight

    MainLayout {
        id: content
        anchors.fill: parent
    }
}
